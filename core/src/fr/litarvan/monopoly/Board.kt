package fr.litarvan.monopoly

object Board
{
    const val textureSize = 4096f
    const val renderSize = 20f

    const val outerBorder = 15f / textureSize * renderSize
    const val innerBorder = 20f / textureSize * renderSize

    const val caseHeight = 510f / textureSize * renderSize
    const val caseWidth = 316.5f / textureSize * renderSize

    const val caseTop = 60f / textureSize * renderSize
}